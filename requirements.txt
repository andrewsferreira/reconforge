# ReconForge Python dependencies
pyyaml>=6.0

# Optional — loot encryption (--encrypt-loot)
cryptography>=41.0

# Optional — testing
# See requirements-dev.txt / pyproject.toml [project.optional-dependencies.dev]
