# ReconForge — Architecture Review

**Date:** 2026-07-11 (P0 remediation pass completed same day — see §11 checklists and the `remediation/p0-release-blockers` branch history for what changed and why)
**Status of the phased mandate** (see [AGENTS.md](../AGENTS.md) for the full 27-phase plan): Phase 1 (this document) — done. Phase 2 (Scope and Positioning) — done. Phase 3 (Project Consolidation) — done. Phase 4 (Secure Process Execution) — done 2026-07-12: `core/runner.py` now has an environment allowlist (was passing the full parent environment, including ReconForge's own secret env vars, to every child tool), working-directory control, output-size limits (10MB/stream, post-capture — not true streaming enforcement, documented as a gap), per-command execution IDs with a structured `AUDIT` JSONL event, `InvalidToolArgumentError`, a tool-version-capture helper, and secret-redaction coverage expanded to cookies/private keys/Kerberos tickets/cloud credentials/DB connection strings (found and fixed two real double-redaction bugs while verifying the new patterns empirically). Not done in Phase 4: true streaming output caps (would need Popen + incremental reads instead of subprocess.run) and wiring `get_tool_version()` into every tool wrapper's constructor (~30 files) — both noted as follow-ups below rather than silently implied complete. Phase 5 (Target Validation and Safety) — done 2026-07-12: `core/validators.py::validate_url()` now rejects embedded userinfo credentials, control characters, and excessive length (was only checking scheme/netloc); wired into `modules/web/web_module.py` and `modules/api/api_module.py`'s `_normalise_url()`, which previously bypassed validation entirely; `core/validators.py::validate_domain()` — previously dead code — is now wired into `ADModule.__init__`'s `domain` parameter; every active (non-`--dry-run`) CLI invocation now requires one of `--authorized-target`, `--lab-mode`, or a validated `--enforce-scope` via `reconforge/cli.py::require_authorization()`, a deliberate breaking change released as `2.0.0` per `docs/VERSIONING.md`; and the pre-existing redirect/discovered-host scope gap for CLI-tool-wrapping modules (`network`/`web`/`api` shelling out to nikto/ffuf/gobuster/sqlmap/nuclei, which follow their own redirects outside ReconForge's visibility) is now documented explicitly in `docs/LIMITATIONS.md` rather than left implicit. Phase 6 (Tool Adapters) — done 2026-07-12: full audit of all 28 `modules/*/tools/*.py` wrappers (via a dedicated research pass reading all 28 files plus `core/runner.py`), fixing 9 confirmed issues — a real command-injection-adjacent bug (`network/tools/smbclient.py`'s `list_share_contents()` built an unsanitized `-c` batch-command string from a `path` argument, allowing `;`-separated smbclient command injection), a plaintext-credential-logging gap (`core/logger.py` didn't redact `-w <password>`, `-U user%password`, or `DOMAIN/user:password` forms — three real credential formats used by ldapsearch/smbclient/impacket wrappers), a silently-wrong success flag (`ad/tools/nmap.py::dns_all_srv()` hardcoded `success=True` regardless of the underlying `dig` calls' actual results), and an `output_file=` clobbering bug affecting 11 tool wrappers across web/api modules (`Runner.run()` unconditionally overwrites `output_file` with captured stdout *after* the subprocess exits, corrupting the real output already written by the tool's own `-o`/`--log-json`/`-oJ` flag — confirmed empirically for curl via a live local-HTTP-server test, reasoned from documented tool-flag semantics for the rest since those binaries aren't installed in this dev environment). Also fixed: magic returncode literals (`-1`/`-2`) colliding with `core.runner`'s `RC_TIMEOUT`/`RC_TOOL_NOT_FOUND` sentinels across 8 files (added a new `RC_PRECONDITION_FAILED` constant for the "no wordlist" class of synthetic result, reclassified hydra's authorization-block result as `RC_POLICY_BLOCKED` since that's what it actually is); dead `self.tool_cfg`/`self._tool_cfg()` wiring in 4 AD wrappers plus one method of `advanced_impacket.py` (timeouts were never actually read from `tools.yaml` despite each docstring claiming they were); two dead `timeout` parameters in `ad/tools/smbclient.py` that were silently discarded instead of forwarded. Explicitly deferred to a documented follow-up rather than fixed in this phase: `validate_arg()` coverage across the 24/28 tool wrappers that don't call it, `get_tool_version()` (added Phase 4) still wired into zero of the 28 constructors, and the httpx-wrapped-three-times duplication — see the P1/P2 checklist below. 37 new tests added (525→578, `pytest -q`); ruff/mypy/bandit all pass. Phase 7 (Parsing/Normalization) — done 2026-07-13: audit of all 26 `modules/*/parsers/*.py` files (via a dedicated research pass reading 22 of 26 in full plus targeted checks on the rest), fixing 8 confirmed issues. Two whole-file-abort crash bugs matching the same "one bad record kills the batch" class found in Phase 6's `output_file=` bug: `api/parsers/nuclei_parser.py` raised an uncaught `AttributeError` on a JSONL line with `"info": null` (ported the `isinstance` guard already present in `web/parsers/nuclei_parser.py`), and `ad/parsers/bloodhound_parser.py` assumed every JSON list entry was a dict across all 5 `parse_*_json()` methods (added `isinstance` guards to skip malformed entries instead of crashing). A live data-loss bug: `ad/parsers/nmap_parser.py`'s NSE-script text-mode regex only matched `"| "`-prefixed continuation lines, silently dropping the `"|_"`-prefixed final line of a script block — exactly where SMB-signing status commonly appears, confirmed reachable via `ad/collectors/dns_collector.py`'s XML-unavailable fallback to text parsing. Two defense-in-depth exception-handling fixes: `network/parsers/nmap_parser.py` was missing the broader `OSError` from its caught-exceptions tuple (had `FileNotFoundError` but not `PermissionError`/`IsADirectoryError`), and `ad/parsers/nmap_parser.py::parse_xml()` never populated its `raw` field on a parse-failure path, leaving zero postmortem-debugging signal on malformed XML. A severity-inconsistency fix: `api/parsers/nuclei_parser.py` reimplemented its own severity map with no alias normalization (`"moderate"`/`"important"`/`"crit"` all silently fell through to `"info"`) while `web/parsers/nuclei_parser.py` — the exact same tool — already used a shared `normalize_severity()` helper; wired the API variant through the same helper. A correctness fix: `network/parsers/smb_parser.py`'s access-denied detection covered fewer NT_STATUS patterns than `ad/parsers/smb_parser.py`'s equivalent (missing `NT_STATUS_ACCOUNT_DISABLED` and bare `ACCESS_DENIED`/`LOGON_FAILURE` without the `NT_STATUS_` prefix), risking misclassifying some denied SMB access as successful; ported the broader pattern list. A scoped-down duplication fix: the LDIF entry-splitting algorithm was independently implemented three times (`ad/parsers/ldap_parser.py`, `ad/parsers/delegation_parser.py` — whose own docstring said "Replicates the pattern from ADLdapParser._split_entries", i.e. self-acknowledged duplication — and `network/parsers/ldap_parser.py`); consolidated the two byte-for-byte-identical AD-module copies into a new `modules/ad/parsers/ldif_utils.py::split_ldif_entries()`, but *not* the network variant, whose case-sensitive attribute lookups (`obj.get("sAMAccountName")`, `obj.get("dNSHostName")`) across ~15 call sites are load-bearing and incompatible with the shared function's lowercased-key convention discovered during implementation — a real scope reduction from the original 3-way consolidation plan, documented rather than forced through. Plus small hygiene fixes: 16 unused `typing` imports removed (ruff `--select F401 --fix`), `web/parsers/gobuster_parser.py`'s docstring corrected (claimed DNS-subdomain extraction support that its leading-slash-only regex can never match), `web/parsers/wpscan_parser.py`'s unreachable `severity: str = "high"` dataclass default corrected to `"low"` (every construction site passes severity explicitly; the old default silently implied "high" was a safe fallback for an uncategorized tier that doesn't exist). Explicitly deferred to a documented follow-up rather than fixed in this phase: systemic confidence/severity assignment inconsistency across the remaining parsers (no evidence-derived-confidence-with-reason pattern like `reconforge/intelligence/engine.py`'s P1-5 fix), return-type inconsistency (dataclass-with-errors vs. dataclass-without vs. bare dict vs. bare list, no shared convention across the 26 files), remaining silent-failure gaps in `netexec_parser.py`/`delegation_parser.py`/`impacket_parser.py`/`enum4linux_ng_parser.py` (still no raw/error preservation on parse failure), and `ad/parsers/impacket_parser.py`'s whitespace-column-splitting heuristic risk on `GetADUsers.py` tables with blank optional columns (flagged as unconfirmed without a real tool-output sample to test against, not fixed speculatively) — see the P1/P2 checklist below. 33 new tests added (578→611, `pytest -q`); ruff/mypy/bandit all pass. Phase 8 (Confidence Model) — done 2026-07-13: audit of `core/findings_manager.py` (the `Finding`/`FindingsManager` model shared by all 5 recon modules), `modules/surface/intelligence/confidence_scorer.py`, and ~94 confidence-assignment call sites across `modules/*/phases/*.py` and `modules/ad/analyzers/*.py`. Two fixes address the same "fabricated high-trust finding from a parsing defect" class of bug this project has now found repeatedly: `web/phases/exploit_candidates.py::_run_sqlmap()` matched the bare substring `"injectable"` with no negation awareness, so sqlmap's own NEGATIVE-result lines ("does not appear to be injectable") tripped the check and fabricated a `severity="critical", confidence="confirmed"` SQL-injection finding for a target sqlmap explicitly reported as not injectable — the single most severe bug found in this phase, now guarded against sqlmap's known negative phrasings. `web/phases/vulnerability_scanning.py` derived nuclei-finding confidence FROM severity (`"high" if sev in ("critical","high") else "medium"`), inverting `core/findings_manager.py::_clamp_severity()`'s intended evidence→confidence→severity-cap flow — any finding severe enough to need capping automatically got confidence high enough to escape it, defeating the clamp mechanism entirely; confidence is now a flat "high" tied to nuclei's own deterministic template-match behavior, independent of the template's rated severity. A semantics fix: `ad/phases/bloodhound_collection.py`'s Domain-Admin attack-path finding used `confidence="confirmed"` for a BloodHound graph-traversal inference — per this file's own docstring, "confirmed" means exploited/verified, which a multi-hop ACL/group-membership inference is not; relabeled to "high" (still uncapped at `critical` severity, since "high" and "confirmed" share the same cap, but no longer overclaims verification). Added a `confidence_reason: str` field to `Finding`/`FindingsManager.add()`/all 5 modules' `add_finding()` wrappers — populated at the three fixed call sites above as adoption examples; broader adoption across the remaining ~90 call sites is deferred (see P2 below). Also fixed: `core/findings_manager.py`'s dead `_CONFIDENCE_RANK` constant removed and `get_heuristic_findings()` (previously defined but never called anywhere in the repo) now wired into `to_markdown()`'s output as a dedicated "Heuristic Findings" section; each of the 4 modules (network/web/api/surface — AD uses a different, `core/reporting/`-based pipeline and was unaffected) that generate `quick_report.md` now surface a visible warning when `FindingsManager.clamped_count` is nonzero, since the report's "Critical & High Findings" section filters by post-clamp severity and a finding that started critical but got downgraded due to weak confidence previously vanished from the headline summary with zero operator-facing signal; `modules/surface/intelligence/confidence_scorer.py`'s `LABELS` threshold table never emitted `"heuristic"` — the weakest tier `FindingsManager.VALID_CONFIDENCES` supports — so a zero-signal service detection (no port/banner/version/multi-detection/HTTP-confirm match at all) was mislabeled `"low"` instead of `"heuristic"`; and `api/phases/discovery.py` marked every ffuf-discovered endpoint `confidence="confirmed"` unconditionally regardless of HTTP status code, while `web/phases/content_enumeration.py` — the same tool, same evidence type — ties confidence to status code; aligned the API variant with the web module's tiering. `core/findings_manager.py` had zero test coverage prior to this phase (not even `_clamp_severity()`'s boundary cases); added a full 25-combination confidence×severity matrix test plus coverage for `add()`'s validation/clamping/warning paths and all getter/serialization methods. Explicitly deferred to a documented follow-up rather than fixed in this phase: the AD module's wholesale `confidence="confirmed"` usage across all remaining ~29 phase/analyzer call sites beyond the one bloodhound fix (many are directly-observed AD/LDAP facts, defensible even if not strictly "exploited/verified"; judgment-heavy relabeling not attempted wholesale), `confidence_scorer.py`'s surface-module-only scoping (not shared with network/ad/web/api despite being the closest thing to a principled evidence-derived confidence model in the recon-module codebase), a handful of other spotted-but-unfixed within-file confidence inconsistencies, and `core/cve_enricher.py::enrich_references()` making a blocking network call from inside `FindingsManager.add()` when `RECONFORGE_NVD_LOOKUP=1` is set, with `add()` called in tight per-share/per-user/per-port loops throughout phase code — see the P1/P2 checklist below. 57 new tests added (611→668, `pytest -q`); ruff/mypy/bandit all pass. Phase 9 (Deduplication/Correlation) — done 2026-07-13: investigated the concrete, reachable impact of `core/findings_manager.py::FindingsManager.add()` having zero identity/dedup check (every call unconditionally appends — an earlier item on this very checklist, "Add finding-level deduplication/correlation," had flagged this as an open gap). Found and fixed two exact-duplicate-producing bugs, both intra-module (not requiring any cross-module interaction to trigger): `network/phases/authentication_checks.py`'s `ANON_TEST_SERVICES` mapped both port 139 and 445 to the same host-level `null_session` test (the underlying `smbclient.null_session_test()` call ignores port entirely), so any host with both ports open — the overwhelming majority of Windows/Samba targets — got the test run and a finding recorded twice; fixed by tracking whether the host-level test already ran. `network/parsers/nmap_parser.py::check_anonymous_access()` independently appended a finding from a service-name heuristic AND from NSE script output containing "anonymous"/"null session", producing two findings describing the same condition for one port; fixed to emit at most one finding per port, preferring the stronger NSE evidence. Built the general safety net these two bugs are instances of: a fingerprint-based exact-duplicate check in `FindingsManager.add()` itself, modeled directly on `core/credential_vault.py::CredentialVault._fingerprint()`'s proven O(1) set-based pattern (key: `finding_type|severity|confidence|target|description`, checked before `enrich_references()` so a repeated call does no wasted work, exposed via a new `duplicate_count` property surfaced in `to_markdown()`). Also closed a structural gap the investigation surfaced: `WorkflowOrchestrator.findings` (a `FindingsManager` instantiated under a `# Aggregated managers` comment) was dead code — never written to or read anywhere in the repo, unlike `CredentialVault`/`LootManager`, which already flow through `credential_vault.ingest_from_loot(mod.loot)` after every module run in `_run_module()`. Added `FindingsManager.ingest(other)` (re-runs each finding through `add()`, so the new exact-duplicate dedup applies automatically — e.g. network and ad modules each independently reporting the identical SMB-signing misconfiguration for the same target during a workflow's auto-handoff now collapse into one entry) and wired it into `_run_module()`'s existing loot-ingestion call sites and the workflow's final report save (`findings_<timestamp>.{json,md}` alongside the existing `vault_<timestamp>.json`). Two `modules/surface/intelligence/correlation_engine.py` bugs found while assessing whether the surface module's existing `ServiceDeduplicator`/`CorrelationEngine` tooling could be reused for Finding-level dedup (verdict: no — they operate on raw pre-Finding service dicts and are the wrong shape, see P2 below): `_ingest_http()` grouped HTTP services by scheme alone ("http"/"https"), ignoring port, so two genuinely distinct services on different ports of the same host (e.g. `:80` and `:8080`) were conflated into one `CorrelatedService`, misattributing their `technologies`/`products`/`urls` to each other — fixed to key by port while still correctly merging into an existing port-scan-detected entry for the same port when one exists (verified both behaviors empirically, since the existing test suite's own assertion had a same-line `or` clause that would have silently passed either way); and `_ingest_entry()` hardcoded `detection_method="port_scan"` for every entry regardless of `ServiceDeduplicator.deduplicate_ports()` having already tagged pre-merged entries with the real union of methods in `_detection_methods`, meaning `ConfidenceScorer`'s `multi_detection` signal (20% weight, `len(detection_methods) >= 2`) could never fire for any TCP/UDP service, only HTTP — fixed to prefer the entry's own tagging when present. Explicitly deferred to a documented follow-up rather than fixed in this phase: the cross-module SMB-signing duplicate case specifically driven by `WorkflowOrchestrator` auto-handoff (now caught by `FindingsManager.ingest()`'s exact-match dedup only when both modules' descriptions happen to be byte-identical, which they currently are not), `CredentialVault._fingerprint()`'s case-sensitive username/domain matching, `LootManager`'s O(n) linear-scan dedup vs. `CredentialVault`'s O(1) set-based approach, and a `network/phases/port_scanning.py` live-hosts loop edge case — see the P1/P2 checklist below. 25 new tests added (668→693, `pytest -q`); ruff/mypy/bandit all pass. Phase 10 (Attack-Path Analysis) — done 2026-07-13: audited the three independent "attack"-related systems in the codebase (`reconforge/attack_paths/engine.py`'s HTTP-endpoint graph/replay engine, `modules/ad/attack_paths/*.py`'s 6 flat template builders, and `modules/surface/intelligence/attack_prioritizer.py`'s unrelated target-ranking system — confirmed no shared types/logic between any of the three, a third instance of the module-duplication pattern already flagged in Phase 3/7; not consolidated, since each is structurally different, not just copy-pasted). Fixed 4 correctness bugs in `reconforge/attack_paths/engine.py`: `_path_impact()` checked the bare string `"auth_bypass"` where every real finding-type constant used throughout the codebase is `"auth_bypass_candidate"` — the branch was dead, silently mislabeling every auth-bypass chain's impact and feeding the wrong `impact_weight` into scoring; `_compatible_findings()`'s cross-parameter cluster allowlist omitted `"auth"`/`"role"`, two canonical clusters `reconforge/intelligence/engine.py::_param_risk()` flags as equally high-risk as `id`/`user_identifier` (`auth_or_role_parameter`), so chains pivoting through an auth/role-parameter correlation — arguably the highest-value cross-endpoint pivots — could never be generated; `_refine_paths()` looked up next-step candidates at a path's last endpoint without excluding the finding that step already chains through, so refinement commonly re-appended a step identical to the existing S3 (padding step count, which lowers the path's own `exploitability` score, and producing an effectively cyclic chain); and `_build_graph()`'s cluster→endpoint edges were appended without ensuring the endpoint node exists (defensive fix — currently safe only by an upstream invariant that isn't itself enforced in `_build_graph()`). Applied the same "no dedup at all" fix Phase 9 built for `FindingsManager` to `core/attack_workflow.py::AttackWorkflow.add_attack_path()`: exact-match dedup by chain name (mirroring `suggest_next()`'s existing pattern), closing 3 concrete double-fire cases found — Kerberoast/AS-REP chains built independently from both LDAP-based identity enumeration and BloodHound collection, "Privileged Account Targeting" hardcoded verbatim in both `identity_enumeration.py` and `privilege_escalation_paths.py`, and "SMB Relay Attack" hardcoded independently in `passive_recon.py` and `acl_paths.py`. The `passive_recon.py`/`acl_paths.py` duplication was also fixed at the root: `self.acl_path_builder` (an `AclPathBuilder` instance) had been constructed in `PassiveReconPhase.__init__` but `.build()` was never called anywhere in the codebase — `_generate_workflow()` hand-rolled a partial copy of the builder's own SMB-relay chain instead, missing the builder's accompanying `ntlmrelayx.py` `NextStepSuggestion`; now calls the real builder and forwards its full output. A second instance of the same dead-builder pattern was found and fixed while cross-checking the other 5 builders' wiring: `modules/ad/phases/bloodhound_collection.py` also constructed a `self.delegation_builder = DelegationPathBuilder()` that `.build()` was never called on — but unlike the ACL case, this one was genuinely dead rather than cheaply wirable: `DelegationPathBuilder` needs `unconstrained_delegation`/`constrained_delegation`/`rbcd` object lists this phase never builds, and the one delegation scenario it does detect (unconstrained delegation via BloodHound computer objects) is already surfaced through `_identify_da_paths()` → `self.acl_builder`'s generic `bloodhound_attack_paths` handling; delegation-specific chains are correctly generated elsewhere by the dedicated `delegation_discovery.py` phase, which already wires the same builder with the right data. Removed the dead instantiation and import rather than force a redundant data-collection path into this phase to justify it. Also fixed: `privilege_escalation_paths.py::_build_password_spray()` embedded a literal password (`'Spring2026!'`) in a ready-to-copy `crackmapexec` command as if it were a real credential rather than a placeholder — replaced with a `passwords.txt` wordlist reference (also more realistic tradecraft, since spraying uses a list, not one hardcoded guess). A stale `"Generated by ReconForge v1.0 release"` footer string (vs. the actual `2.2.0`) was hardcoded independently in 8 report-generating files across `core/` and 4 recon modules (`attack_surface_reporter.py`, `ad_summary_reporter.py`, `core/engagement.py`, and the network/web/surface/api module reports) — added a single `core/version.py::__version__` constant (filling a gap `docs/VERSIONING.md` had explicitly flagged as not yet existing) and wired all 8 footers to it, plus a regression test asserting it stays in sync with `pyproject.toml`. Two pre-existing broken doc cross-references were found and fixed while editing the surrounding text: `reconforge/attack_paths/engine.py`'s docstrings pointed to `docs/CONFIDENCE_MODEL.md` and `docs/ATTACK_PATHS.md`, neither of which exists (the real files are `docs/FINDINGS.md` and `docs/attack_path_generation.md`). `docs/attack_path_generation.md` and both CLI entrypoint docstrings (`reconforge/burp/attack_paths.py`, `reconforge/entrypoints/attack_paths.py`) used unqualified "validated attack paths" language that P1-4's code fix (the `unreachable`/`reachable`/`corroborated` tiering) had already walked back in `engine.py`'s own docstrings but never propagated to the surrounding docs — reworded to match the honest tiering and its "heuristic replay, not confirmed exploitation" caveat. `modules/ad/attack_paths/*.py` (6 builders + base dataclasses) had zero test coverage prior to this phase (confirmed via repo-wide grep — the one existing AD test file that touches attack paths deliberately stubs the builders out with `SimpleNamespace`); added 21 tests covering each builder's chain-generation branches and empty-input no-op case directly. Explicitly deferred to a documented follow-up rather than fixed in this phase: extending the attack-path graph to cover AD/network assets beyond HTTP endpoints (pre-existing P2 item, unchanged), the third `attack_prioritizer.py` system's continued isolation from the other two, and cross-module attack-path chains that are semantically similar but differently *named* (the new `add_attack_path()` dedup is exact-match-on-name only, the same scoping choice Phase 9 made for `FindingsManager`) — see the P1/P2 checklist below. 33 new tests added (693→726, `pytest -q`); ruff/mypy/bandit all pass. Phase 11 (AD/Web/API Module Quality) — done 2026-07-13: audited the phase-orchestration logic (not tool wrappers/parsers/confidence assignment, already covered) in all 5 `modules/ad/phases/*.py`, 4 `modules/web/phases/*.py`, and 4 `modules/api/phases/*.py` files. Found and fixed a false-negative bug that's the mirror image of this project's usual "fabricated finding" class: `bloodhound_collection.py::_identify_da_paths()`'s Kerberoast→DA and AS-REP→DA branches checked group membership via `_is_privileged_group(gid)`, matching readable substrings like `"domain admins"` against `gid` — but `user.member_of` holds BloodHound `ObjectIdentifier` SIDs (`S-1-5-21-...-512`), never readable names, per `bloodhound_parser.py`'s own parsing. The check could never match, so both branches never fired for any target, regardless of input — the phase looked like it was checking group membership but structurally couldn't. Fixed to check membership against `results["da_users"]`, the SID-keyed list `PrivilegeAnalyzer` already builds from the same "Domain Admins" group data; the now-unreachable `_is_privileged_group()` helper was removed. Found and fixed a genuine evidence-misattribution bug in `api/phases/fuzzing.py`: `_classify_error_response()`/`_check_for_info_disclosure()` searched ffuf's whole-batch `stdout` (the only body-content text available — ffuf's JSON output format captures no per-result response body) but reported a pattern match as "evidence" for one arbitrarily-chosen entry's specific URL, so entry A's SQL-error text could get reported as evidence for entry B's unrelated 500. A dead `response_body = entry.content_type or ""` variable (assigned, never used) was the tell that per-entry evidence was intended but never actually wired up. Fixed to classify the batch text once per fuzz run and report a single finding covering every affected entry, honest in its evidence text that ffuf's instrumentation can't isolate the specific triggering input — same fabricated-evidence shape as the sqlmap negative-phrasing bug (Phase 8) and `dns_all_srv()` (Phase 6), different call site, only surfaced now because this is the first phase to audit `modules/api/phases/*.py` orchestration logic. Found and fixed a `success=True`-regardless-of-outcome bug one layer below the phase: `DelegationCollector.collect()` hardcoded `result.success = True` unconditionally, while each of its three LDAP queries (unconstrained/constrained/RBCD) silently returns `[]` on tool-unavailable, opsec-block, missing base DN, *or* a failed LDAP run alike — making `delegation_discovery.py`'s `if collected.success:` gate an unconditionally-true dead branch, and a total LDAP-bind failure indistinguishable from a genuinely clean environment. Fixed each query to report whether it actually completed; `collect()`'s `success` is now true only if at least one query (or findDelegation.py) really ran, with the phase now surfacing an explicit finding and returning early — matching the honest pattern `bloodhound_collection.py` already used — rather than silently continuing through loot/analysis/attack-path steps on empty data that looks identical to a clean scan. Found and fixed a smaller instance of the same silent-failure-as-success class: `web/phases/surface_discovery.py::_run_wafw00f()` was the only tool-invoking method in that file that didn't check `run_result.success` before trusting parsed output (contrast `_run_whatweb()`, two methods above it, which does) — a wafw00f execution failure was indistinguishable from a genuine "no WAF detected" finding; added the same success check. Explicitly deferred to a documented follow-up rather than fixed in this phase: `results["success"]` is decorative (set `True` unconditionally regardless of underlying outcome) in 12 of 13 phase files — `bloodhound_collection.py` and now `delegation_discovery.py` are the only two that make it a real signal; inert today since no caller (`ad_module.py`/`web_module.py`/`api_module.py`/`workflow_orchestrator.py`) reads any phase's `results["success"]`, but a latent trap for future code that starts trusting it. Also deferred: the weak SSRF-confirmation regex signal-to-noise in `web/phases/vulnerability_scanning.py` (already hedged `confidence="low"`, not a fabrication, just noisy) — see the P1/P2 checklist below. 22 new tests added (726→748, `pytest -q`); ruff/mypy/bandit all pass. Phase 12+ not started.
**Scope:** Full repository audit prior to public release, per the ReconForge engineering mandate (see [AGENTS.md](../AGENTS.md)).
**Method:** Static review of `core/`, `modules/`, `reconforge/`, `mcp_validation/`, `tests/`, `docs/`, `.github/`, and packaging config, cross-checked against actual command execution (`pytest`, `ruff check`, `mypy`, `bandit`) rather than documentation claims alone.
**Status of this document:** living. Update it as remediation lands; do not delete resolved items — mark them done and keep the history.

---

## 1. Current Architecture (as it actually runs today)

The **real, working, end-to-end path** is:

```
reconforge/cli.py (argparse dispatcher)
  -> enforce_scope_gate()          [core/authorization_gate.py, opt-in via --enforce-scope]
  -> modules/{network,ad,web,api,surface}/<name>_module.py
       -> phase classes (modules/<name>/phases/*.py, modules/<name>/base.py)
            -> tool wrappers (modules/<name>/tools/*.py)
                 -> core/runner.py (Runner.run) — the ONE subprocess execution layer
            -> parsers (modules/<name>/parsers/*.py)
       -> core/findings_manager.py (FindingsManager)
       -> module-local quick_report.md (OutputManager)
```

`core/workflow_orchestrator.py` (`WorkflowOrchestrator`) is a second layer that chains the same five modules together for the `reconforge workflow` subcommand, feeding a shared `WorkflowContext` and driving `FindingsManager`, `CredentialVault`, `LootManager`, `AttackWorkflow`, `core/ai_orchestration.py`, and `core/post_exploitation.py`.

A **separate, narrower path** exists for Burp Suite MCP integration: `reconforge/burp/*.py` → `reconforge/entrypoints/*.py` → `core/adapters/burp/*` → `core/orchestrator/execution_coordinator.py` → `core/policy/scope_policy.py`. This path uses a completely different orchestration and scope-enforcement stack than the recon modules above (see §5, Duplicated Components).

`core/attack_workflow.py` is not an orchestrator — it's a kill-chain/hypothesis log (`AttackWorkflow.add_step/record_result/add_attack_path`) used by every module's phase base class to produce narrative Markdown. The name collision with `core/workflow_orchestrator.py` and `core/orchestrator/workflow_engine.py` is confusing but the three files are not functionally competing (the third, `workflow_engine.py`, is dead — see §8).

### What the CLI actually installs

`pyproject.toml` registers `reconforge = "reconforge:main"`. `reconforge/__init__.py` re-exports `main` from `reconforge/cli.py`. The installed CLI never imports `core/orchestrator/`, `core/policy/`, or `core/schemas/` except transitively through the Burp subcommands.

---

## 2. Strengths

These should be preserved and are worth highlighting publicly — they are genuine evidence of engineering discipline:

- **No `shell=True` anywhere.** A repo-wide grep across ~36k LOC found zero uses of `shell=True`, `os.system`, `os.popen`, `eval`, or `exec`. Every subprocess call goes through one function, `core/runner.py::Runner.run()`, using `subprocess.run(list[str], ...)`.
- **Severity and confidence are genuinely separated.** `core/findings_manager.py` implements explicit confidence levels (`confirmed/high/medium/low/heuristic`) with a documented, enforced severity cap table (`_CONFIDENCE_SEVERITY_CAP`) — a `heuristic`-confidence finding cannot present as `critical` severity. This is exactly the model the task brief asks for, already partially built.
- **The one HTML report path is safe.** `core/output_manager.py::_write_html_report` runs `html.escape()` over the entire rendered document before embedding it — no injection route was found. No CDN references, no inline unescaped tool output.
- **`core/runner.py` has real operator-safety features**: timeout handling, a dry-run mode, a kill-switch (env var/file based), structured JSONL audit events, and binary-existence checks at ~29 call sites.
- **`AGENTS.md` documents a specific, non-generic operating contract** — scope/policy enforcement must live in code, not prompts; LLMs are restricted to summarization/classification and explicitly barred from execution/scope decisions. This is unusual rigor for how the project was built, even though the current state doesn't yet meet its own bar (see §6).
- **`docs/LIMITATIONS.md`, `docs/ARCHITECTURE.md`, `docs/TERMINOLOGY.md`, `docs/SEVERITY_CRITERIA.md`, `docs/SUPPORT_MATRIX.md`** are specific and largely honest about scope — they explicitly disclaim being an exploitation framework and document OPSEC gating. These are the strongest docs in the repo.
- **`CONTRIBUTING.md` and `CHANGELOG.md`** are project-specific, not boilerplate, and reference real file paths and conventions.
- **Real parser test coverage exists** for the well-covered tools (nmap, ffuf, arjun, impacket, nuclei-api, and the web module's ffuf/gobuster/nikto/nuclei/wafw00f/whatweb) — tests exercise actual parsing logic against fixture strings, not just mocks.
- **`core/reporting/exporters/manifest_builder.py`** implements a real SHA-256 hash-chain over report artifacts — a genuine integrity feature, even though the pipeline it belongs to is currently unreachable from the CLI (see §8).

---

## 3. Architectural Weaknesses

- **Two parallel orchestration architectures that don't interoperate.** The real recon flow (§1) and a second, generic orchestration stack (`core/orchestrator/execution_coordinator.py`, `module_router.py`, `workflow_engine.py`, `core/policy/scope_policy.py`, `core/schemas/contracts.py`) coexist. The second stack is only reachable through the Burp subcommand and is otherwise exercised solely by `tests/core_foundation/*`. A contributor reading the codebase has no way to tell which is canonical without tracing imports.
- **Two independent scope/authorization mechanisms** with no shared code: `core/authorization_gate.py::ScopeAuthorization` (used by the CLI's recon subcommands) vs. `core/policy/scope_policy.py::ScopePolicy` (used only by the Burp path). A scope bug fixed in one does not protect the other.
- **Three reporting pipelines, two of them dead**: per-module hand-rolled `quick_report.md` (actually runs), `core/output_manager.py::generate_engagement_report()` (zero callers outside its own definition, no test), and the structured `core/reporting/` "Phase 5" pipeline (only reachable via `OutputManager.generate_reporting_bundle()`, which is itself never called in production — only by its own test).
- **Attack-path modeling is narrower than the product's stated scope.** `reconforge/attack_paths/engine.py` builds a graph of only `endpoint`/`parameter`/`finding`/`cluster` nodes (HTTP-only). The AD module has no representation in any attack graph — no asset/user/group/credential/session/privilege/trust nodes exist anywhere, despite AD being one of the five advertised modules.

---

## 4. Security Risks

Ordered by severity; full remediation ownership in §10.

| # | Risk | Evidence | Severity |
|---|------|----------|----------|
| S1 | Target hostname validation accepts **any** string unconditionally — no rejection path exists | `core/target_parser.py:47-52` — the regex match branch and the no-match branch both execute `target.hostname = target_str` and return normally. This is the parser actually imported by `modules/network`, `modules/ad`, `modules/surface`. | **P0** |
| S2 | Scope/authorization is never enforced at the point commands actually execute | `core/runner.py` contains zero references to `authorization_gate`, `ScopeAuthorization`, `ScopeValidator`, or `DomainScopeValidator`. The one component that would enforce scope deterministically before execution (`ExecutionCoordinator` + `ScopeValidator`) is instantiated only in `tests/core_foundation/test_execution_coordinator_foundation.py`. | **P0** |
| S3 | No re-validation of scope for redirects or newly discovered targets in the `core/runner.py`-based recon path | Scope was checked once, at CLI parse time, against the original `--target` string only. No code path re-checked a target after discovery of a new host/asset mid-run (fixed 2026-07-11 for `WorkflowOrchestrator` auto-handoff — see §11 P0 checklist). ~~`reconforge/intelligence/engine.py`'s `HttpMutationEngine` issues new requests with no scope check on mutated URLs~~ — **this part of the original finding was incorrect**, corrected 2026-07-11: every request the engine issues goes through `HttpCollector` → `BurpMcpProvider.send_http1_request/send_http2_request`, which calls `_enforce_request_scope()` (fail-closed `DomainScopeValidator`) before executing anything. Proven end-to-end in `tests/intelligence/test_vulnerability_engine_scope.py`. | **P0 (closed)** |
| S4 | `--enforce-scope` is opt-in (default off on all six subcommands), single-shot, and does exact-string matching only (no CIDR/domain-suffix matching) | `reconforge/cli.py:324-337`, `core/authorization_gate.py:52-62` | **P1** |
| S5 | Command strings — potentially containing target-supplied or credential-adjacent data — flow unredacted into persisted logs | `Runner._command_log` → `save_command_log()` and `NotesManager.add_command_note()` bypass `core/logger.py::sanitize_log()`, which is only applied on the JSONL audit stream and inside specific `command()`/`loot()`/`credential()` helpers. | **P0** |
| S6 | `validate_arg()` shell-metacharacter guard is applied inconsistently | Only used in `modules/api/tools/{arjun_tool,ffuf_api,httpx_tool,nuclei_api}.py`. Zero uses across `modules/network` (19 files), `modules/web` (27 files), `modules/ad` (58 files). No leading-`-` flag-injection guard anywhere. | **P2** |
| S7 | Credential vault / loot manager: encryption optional (default off), Fernet key stored at a fixed predictable path next to the data it protects, silent plaintext fallback if `cryptography` is missing | `core/credential_vault.py`, `core/loot_manager.py` — keys at `~/.reconforge/{vault,loot}.key`, chmod 0600, adjacent to the encrypted file itself. | **P1** |
| S8 | Two silently-swallowed `except Exception: pass` blocks | `core/secrets_manager.py:48`, `modules/surface/parsers/surface_parser.py:160` | **P2** |
| S9 | `core/validators.py` — the correct, strict target validators (RFC952/1123 hostname regex, IP/CIDR/URL/domain checks) — is entirely unused; no module imports it | Confirmed via repo-wide grep for `validate_target`, `validate_hostname`, `from core.validators`. | **P1** |
| S10 | Risk policy engine disabled by default | `core/risk_policy.py` requires `RECONFORGE_POLICY_ENFORCE=1`; off by default means the one policy gate inside `Runner.run()` does nothing unless an operator knows to set an env var. | **P2** |
| S11 | Unexplained 255KB Fernet-encrypted blob tracked in git since the initial commit, not referenced anywhere in code/docs/config | `.abacus.donotdelete` — verified via `file`, `git log`, and repo-wide grep for "abacus" (zero references). Content and origin unknown; likely a platform artifact, but must be identified and either documented or removed before any public push. | **P0** |

---

## 5. Duplicated Components

| Component A | Component B | Notes | Severity |
|---|---|---|---|
| `core/authorization_gate.py` | `core/policy/scope_policy.py` + `core/policy/target_scope.py` | Three independent scope-checking implementations, two of them (`scope_policy.py`, `target_scope.py`) duplicating allow/deny/subdomain logic between themselves too. | P1/P2 |
| `modules/network/tools/{nmap,smbclient,ldapsearch}.py` | `modules/ad/tools/{nmap,smbclient,ldapsearch}.py` | Same binaries wrapped twice with parallel classes and separate parsers (`modules/network/parsers/*` vs `modules/ad/parsers/*`). A flag/CVE fix to one will not propagate to the other. | P2 |
| `core/adapters/burp/*` (wired into `reconforge burp` subcommands) | `mcp_validation/burp/*` | `mcp_validation/` is a ~600+ LOC standalone second Burp MCP client, imported only by its own test (`tests/mcp_validation/test_burp_validator.py`), never wired into the CLI or `pyproject.toml`. It also contains a real syntax error (`mcp_validation/run_validation.py:30,35`) that currently breaks `ruff check .` and `mypy .`. | P1 |
| Per-module `quick_report.md` (live) | `OutputManager.generate_engagement_report()` + `core/reporting/` "Phase 5" pipeline (both dead) | See §3 and §8. | P1 |
| `core/config_loader.py` / `core/tool_config.py` / `core/profile_loader.py` | — | **Not flagged** — reviewed and confirmed to be an intentional layering (raw loader → typed tool view → OPSEC profile view), not duplication. | — |

---

## 6. Misleading / Unverified Claims

These directly contradict the "honest claims" priority in the engineering mandate and must be corrected before public release.

| Claim | Reality | Location |
|---|---|---|
| "375/375 tests passing" | Actual collected test count is **445**, with **4 failing** (`tests/test_cli_surface_encrypt_loot.py`, `tests/test_scope_gate_cli_p10.py`), verified by running `pytest --collect-only -q` and `pytest -q` directly. No two documents in the repo agree: README says 375, `CHANGELOG.md`/`CONTRIBUTING.md` say 348, `docs/FINAL_STABILIZATION_REPORT.md` says 375, `docs/INDEPENDENT_SECURITY_ASSESSMENT_2026-04-13.md` says 383. | `README.md:5,142,190` and 4 other files |
| Quick Start commands work as documented | `python reconforge.py network --target ...` fails — `reconforge.py` was deleted in commit `9bc56bc` when the code moved into the `reconforge/` package. Verified directly: `No such file or directory`. Occurs 25 times across README + docs, and leaks into the installed CLI's own `--help` epilog. | `README.md`, `docs/ARCHITECTURE.md`, `docs/USAGE.md`, `docs/MODULES.md`, `docs/FAQ.md`, `docs/WORKFLOW_GUIDE.md`, others |
| "Internal use — see project documentation for terms" (License section) | No terms exist anywhere in the repo. No LICENSE file. This line is incompatible with a public open-source release — as written, nobody may legally fork, use, or contribute to the project. | `README.md:193-195` |
| `docs/PROJECT_SCORECARD.md` self-score of 9.5/10; `docs/INDEPENDENT_SECURITY_ASSESSMENT_2026-04-13.md` styled as an "Avaliação Profissional Independente" (Independent Professional Assessment), scored 7.8/10 | No named evaluator, no stated methodology, no evidence of third-party review. Reads as an AI-generated self-review presented as external validation, including a fabricated-sounding "383 testes passando localmente" claim that doesn't match the actual test count. Publishing these as-is is a credibility risk. | `docs/PROJECT_SCORECARD.md`, `docs/INDEPENDENT_SECURITY_ASSESSMENT_2026-04-13.md` |
| `docs/SUPPORT_MATRIX.md` — "Docker ✅ Supported" | No `Dockerfile` exists anywhere in the repository. | `docs/SUPPORT_MATRIX.md:33` |
| CI quality gates pass | Running the exact commands from `.github/workflows/quality-gates.yml` locally: `mypy` fails (`Cannot read file 'reconforge.py'`), `bandit` skips the same missing file and separately reports 67 low/8 medium/1 high findings, `ruff check .` fails on a real syntax error in `mcp_validation/run_validation.py`. **The pipeline as configured would not currently pass on `main`.** | `.github/workflows/quality-gates.yml:30,33` |
| `docs/FINAL_STABILIZATION_REPORT.md` verdict "READY FOR DOCUMENTATION" | Correctly marked "HISTORICAL DOCUMENT" at the top (good practice), but predates the current 445-test/broken-CI state and could mislead a reader skimming for current status. | `docs/FINAL_STABILIZATION_REPORT.md` |

Note: `README.md` itself, independent of the two self-assessment docs, is comparatively restrained — no "enterprise-grade"/"autonomous"/"fully automated" language was found in it. The overclaiming problem is concentrated in `PROJECT_SCORECARD.md` and `INDEPENDENT_SECURITY_ASSESSMENT_2026-04-13.md`, and in the stale test-count line.

---

## 7. Untested Assumptions

- That `--enforce-scope` protects operators from scanning out-of-scope infrastructure — false in practice for redirects, DNS-resolved hosts, and any target discovered mid-run (§4, S3).
- That a fixed-path Fernet key stored beside its own ciphertext constitutes meaningful secret protection against any threat model beyond "opportunistic file browsing" — it does not protect against local compromise, and this limitation is not documented anywhere.
- That a non-erroring HTTP response during attack-path "validation" confirms the underlying vulnerability claim. `reconforge/attack_paths/engine.py::_validate_paths` only checks `response_status != 0` (i.e., the request didn't time out/error) before marking a path `validated=True`; `_prioritize_path` then sets `reliability=1.0` and factors it directly into a `critical` priority score. An IDOR or auth-bypass "candidate" that merely returns HTTP 200 on a mutated request can surface in a report as a validated, critical attack path without ever confirming unauthorized data access actually occurred.
- That the mocked unit-test suite (real parser logic, but `Runner.run()` always mocked) demonstrates the tool adapters work against real tool output. No integration tests against real installed binaries were found, and there is currently no local lab (Phase 17 of the engineering mandate) to validate this end-to-end. This is a real gap, not a documentation gap — it should be labeled honestly rather than implied by "445 tests passing."
- That `reconforge/intelligence/engine.py`'s hardcoded per-rule confidence constants (0.6–0.85 for things like `IDOR_candidate`, `auth_bypass_candidate`) reflect actual evidentiary strength. They are fixed literals keyed to a response-length/status-code heuristic, not derived from confirmed evidence — an unconfirmed "candidate" can reach `critical`/`high` priority downstream.

---

## 8. Dead Code

| Item | Evidence | Recommendation |
|---|---|---|
| `core/orchestrator/` (`execution_coordinator.py`, `module_router.py`, `workflow_engine.py`) + `core/policy/scope_policy.py` + `core/schemas/contracts.py` | Reachable only via the Burp subcommand path and `tests/core_foundation/*`; not part of the five recon modules' data path | Either wire into the real recon flow as the canonical execution layer, or delete and consolidate on `core/runner.py` + `core/authorization_gate.py`. Do not keep both indefinitely. |
| `core/output_manager.py::generate_engagement_report()` | Zero callers outside its own definition; no test file | Delete, or wire in as the canonical multi-module report and delete the per-module `quick_report.md` duplication instead. |
| `core/reporting/` package (`pipeline.py`, `models.py`, `exporters/`, `renderers/`) | Only reachable via `OutputManager.generate_reporting_bundle()`, itself never called except by `tests/reporting/test_reporting_pipeline_phase5.py` | This is the most structurally sound reporting code in the repo (has a real hash-chain manifest). Recommend wiring this in as the canonical reporting pipeline (Phase 16 of the mandate) rather than deleting it. |
| `mcp_validation/` (entire package) | Orphaned duplicate of `core/adapters/burp/*`; contains a syntax error breaking CI | Delete. `reconforge/burp/*` + `core/adapters/burp/*` is the wired, tested path. |
| `core/orchestrator/workflow_engine.py` | 26-line frozen-dataclass pair, zero execution logic | Delete as part of the `core/orchestrator/` decision above. |
| `core/exceptions.py` typed exception hierarchy (`ToolNotFoundError`, `ExecutionError`, timeout error) | Only reachable via `run_or_raise()`/`check_tool_or_raise()`, which have zero production or test call sites; real call sites check magic negative return codes instead | Wire these into `Runner.run()`'s actual call sites (this is explicitly required by the engineering mandate: "never use broad exception handlers... convert errors into typed, meaningful exceptions"). |
| `core/validators.py` | Correct, unused (§4, S9) | Wire into `core/target_parser.py` and delete the broken inline hostname check, rather than deleting the validators. |

---

## 9. Incomplete / Overstated Features

- **AD attack-graph coverage**: the AD module (`modules/ad/`) is one of five advertised modules, but no attack-path graph node/edge type represents AD objects (users, groups, credentials, sessions, trusts, delegation). Attack-path generation only exists for HTTP endpoints/parameters.
- **Finding model field gaps**: `core/findings_manager.py`'s `Finding` has `id, finding_type, severity, confidence, target, module, phase, description, evidence, recommendation, references, timestamp`. Missing relative to the mandate's target schema: `title`, `source_tool`, `affected_asset`, `port`, `protocol`, `service`, `raw_evidence_reference`, `confidence_reason`, `status` (open/resolved/false-positive lifecycle), `attack_technique`, `cwe`, `capec`, `mitre_attack`, `execution_id` (on the finding itself, not just the wrapping contract), `parser_version`.
- **No finding-level deduplication/correlation.** Real correlation logic exists for web/API vulnerability classification (`reconforge/intelligence/engine.py::_apply_correlation_boost`, which correctly preserves prior evidence when boosting confidence) and for credential fingerprinting (`core/credential_vault.py`), but `core.findings_manager.Finding` objects have no merge/dedup key at all — the same issue detected by two tools produces two disconnected findings.
- **Tool adapters with parsing logic inlined into phase files instead of a dedicated, tested parser module**: `modules/web/tools/sqlmap.py` (parsed inline in `modules/web/phases/exploit_candidates.py`), `modules/network/tools/hydra.py` (parsed inline in `modules/network/phases/authentication_checks.py`), `modules/api/tools/httpx_tool.py` (parsed inline across `modules/api/phases/discovery.py`, `authentication.py`, `authorization.py`). Functional, but below the parser-test coverage bar and inconsistent with the rest of the codebase's pattern.
- **No reproducible local lab exists yet** (Phase 17 of the mandate) — there is no `demo/`, `labs/`, or `examples/` environment that lets a reviewer run a complete, safe ReconForge assessment against a known target locally. This is required before the project can back up its "445 tests passing" claim with anything beyond mocked unit tests.

---

## 10. Public-Release Blockers (P0) — Summary

These must be resolved before the repository is made public, in this order (each unblocks or de-risks the next):

1. **Licensing**: add a real LICENSE file (Apache-2.0 or MIT recommended), remove the "Internal use" line from README, add third-party tool disclaimer and ethical-use policy (Phase 24).
2. **Unexplained tracked artifact**: identify the origin and purpose of `.abacus.donotdelete`; remove it from the repo (and from git history if it contains anything sensitive) unless it is documented and demonstrably necessary.
3. **Broken documentation**: fix all 25 `reconforge.py` references (README + 6 docs files) to the real entrypoint, and correct the test-count claim everywhere it appears (`README.md:5,142,190`, `CHANGELOG.md`, `CONTRIBUTING.md`, `docs/FINAL_STABILIZATION_REPORT.md`, `docs/INDEPENDENT_SECURITY_ASSESSMENT_2026-04-13.md`) to the actual, currently-passing number.
4. **Fix the 4 failing tests** so the corrected test-count claim is true.
5. **Fix `core/target_parser.py`'s hostname validation** (S1) — wire in `core/validators.py`'s real hostname/IP/CIDR/URL checks and remove the unconditional fallback that accepts any string.
6. **Wire scope/authorization enforcement into `core/runner.py`** (S2) so it applies at every command execution, not just once at CLI-parse time, and re-validate scope for redirects/discovered targets (S3).
7. **Fix command-log/session-note redaction** (S5) so raw command strings pass through `sanitize_log()` before persistence.
8. **Fix CI itself**: correct the mypy/bandit target paths, remove `mcp_validation/` (or fix its syntax error) so `ruff check .` passes, and confirm the full pipeline is green before claiming it in any README badge.

---

## 11. Prioritized Remediation Plan

### P0 — Blocks public release — all closed 2026-07-11, see `remediation/p0-release-blockers`
- [x] Add LICENSE (Apache-2.0), fix README license section, add SECURITY.md with ethical-use/disclosure policy — §10.1
- [x] `.abacus.donotdelete` untracked from git (kept on disk pending owner confirmation of purpose; not deleted) — §10.2
- [x] Fix all `reconforge.py` references across README + docs + CLI (25 occurrences); added `reconforge/__main__.py` so `python -m reconforge` also works — §10.3
- [x] Reconcile and correct test-count claims across all affected documents (445, verified) — §10.3
- [x] Fix the 4 currently-failing tests (root cause: tests loaded the deleted top-level `reconforge.py`) — §10.4
- [x] Fix `core/target_parser.py` hostname validation bypass (now wired to `core/validators.py`); added rejection tests for shell metacharacters, leading `-`, newline/null-byte injection, oversized input — §4 S1, §10.5
- [x] Enforce scope/authorization inside `core/runner.py` at construction *and* every `run()` call (catches mid-scan approval expiry too); wired through all 5 modules, `WorkflowOrchestrator`, and CLI — §4 S2, §10.6
- [x] Re-validate scope for the one concrete "discovered target" mechanism in the recon-module codebase — `WorkflowOrchestrator` auto-handoff now propagates scope to every spawned module, including handoff steps; proven with an end-to-end test. Separately verified (2026-07-11) that `reconforge/intelligence/engine.py`'s HTTP mutation path was never actually missing scope enforcement — see "New follow-ups" below, this was a correction to the original finding, not a new fix — §4 S3, §10.6
- [x] Route command logs and session notes through `sanitize_log()` before persistence; also fixed a real redaction-order bug found while testing (`Authorization: Bearer <token>` was only partially redacted) — §4 S5, §10.7
- [x] Fix CI so it actually passes: corrected mypy/bandit file targets, fixed `mcp_validation/run_validation.py`'s syntax error *and* its actually-broken `main()` (it never called the validator), triaged all 76 bandit findings surfaced once bandit was actually scanning the real package (fixed a real SSRF-ish scheme-override bug in the Burp MCP adapter along the way), added an honest `[tool.coverage]` gate (50%, matching real ~52% coverage — the previous 85% was never true and was never enforced anywhere), fixed a packaging-smoke-test fragility (`--no-build-isolation` failing on any environment without setuptools/wheel pre-installed) — §10.8

**New follow-ups discovered while fixing the above** (not previously in this document):
- **Correction (2026-07-11), not a new gap:** the original finding that `reconforge/intelligence/engine.py`'s HTTP mutation engine "issues new requests with no scope check on mutated URLs" was investigated in P1 and found to be **wrong**. Traced the real call chain: `VulnerabilityIntelligenceEngine.run()` → `HttpCollector.collect_request()` → `BurpMcpProvider.send_http1_request/send_http2_request()`, and the provider calls `_enforce_request_scope()` — a fail-closed `DomainScopeValidator` (empty/non-matching allow-list blocks the request) — before executing *any* HTTP call, baseline or mutated. Confirmed the same wiring holds for `reconforge/entrypoints/{burp_intelligence,attack_paths,burp_web_validation}.py`, all of which pass `--allow-domain`/`--deny-domain` through to `BurpMcpConfig.scope_allowed_domains/scope_denied_domains`. Added `tests/intelligence/test_vulnerability_engine_scope.py` to lock this in end-to-end (not just at the provider level, which `tests/core/adapters/test_burp_scope_enforcement.py` already covered). No code change was needed — this was a documentation error in the original audit, now fixed here.
- Coverage is genuinely ~52%, not 85% — raising it is real, substantial work, tracked as a new P1 item below, not something fixed in this pass.
- `mcp_validation/` was previously mischaracterized in this document as a fully orphaned duplicate of `core/adapters/burp/*`. On closer reading it's mostly a thin, legitimate re-export/wrapper around `core.adapters.burp.*` plus its own `ValidationReport` model and CLI runner — a real, distinct operational validation harness, just not wired into the main `reconforge` CLI as a subcommand. It was repaired (was non-functional — see mcp-validation fix commit), not removed. The "consolidate or remove" P1 item below is revised accordingly.

### P1 — Major quality/security issues
- [x] Consolidate the two orchestration architectures — resolved 2026-07-12 (Phase 3): `core/orchestrator/*` (execution_coordinator, module_router, workflow_engine), `core/policy/scope_policy.py`, and `core/adapters/{base_adapter,contracts,mcp_adapter}.py` were confirmed fully dead (zero real callers, verified with a full call-site sweep) and deleted, along with `tests/core_foundation/*`. The real orchestration path (`reconforge/cli.py` → `modules/*` → `core/runner.py`, plus `WorkflowOrchestrator`) was never affected — §3, §8
- [x] Consolidate the two scope/authorization mechanisms — resolved 2026-07-12 (Phase 3): not consolidated, and documented why. `core/authorization_gate.ScopeAuthorization` (gates subprocess tool execution via `core/runner.Runner`) and `core/policy/target_scope.DomainScopeValidator` (gates outbound HTTP requests via `core/adapters/burp/provider.BurpMcpProvider`) protect genuinely different resource types with different threat models; merging them would force an artificial abstraction. `core/policy/scope_policy.py` — the actually-dead third mechanism — was deleted (see above) — §4 S4, §5
- [x] Decide whether `mcp_validation/` should become a `reconforge burp` subcommand or stay a separate operational harness — resolved 2026-07-12 (Phase 3): kept as a standalone tool (repairing/converting it to a subcommand right now would duplicate `reconforge burp validate`'s purpose); fixed its discoverability instead — linked from README's Documentation table via a new `docs/BURP_MCP_INTEGRATION.md` row — §5, §8
- [x] ~~Add a scope check to `reconforge/intelligence/engine.py`'s HTTP mutation/attack-path path~~ — not needed, this was already correctly enforced; see the correction in P0 §11 "New follow-ups"
- [ ] Raise real test coverage from ~52% back toward 85% — the CI gate is now honest (50%) rather than silently failing, but closing this gap is real, substantial work, not yet done — new item, see P0 §11 "New follow-ups"
- [x] Decide the fate of the two dead reporting pipelines — resolved 2026-07-12 (Phase 3): `OutputManager.generate_engagement_report()`/`generate_reporting_bundle()` (zero callers anywhere, including tests) deleted along with their only helper, `_write_html_report()`, and the unused `reporting_root()`/`reporting_layer_dir()` directory helpers. `core/reporting/` itself (pipeline, exporters, renderers, manifest builder) is real, well-built, tested code — kept, but `docs/REPORTING_ARCHITECTURE.md` now says plainly that nothing wires it to live module data yet; adopting it needs a real mapper from `Finding`/`RunResult`/`AttackWorkflow` into `core/schemas/contracts.py`'s dataclasses, tracked as future work, not done here — §3, §8
- [x] Wire `core/validators.py` into actual target parsing everywhere — resolved 2026-07-12 (Phase 5): `core/target_parser.py` already wired in as of P0-6; remaining bespoke call sites were `modules/web/web_module.py` and `modules/api/api_module.py`'s `_normalise_url()` (previously a no-op prefix-only check) and `ADModule.__init__`'s `domain` parameter (previously zero validation, `validate_domain()` was dead code). `validate_url()` was also hardened first — it only checked scheme/netloc, so it would have passed embedded credentials and control characters through even once wired in. All three now route through `core/validators.py` — §4 S9, §8
- [x] Fix attack-path "validated" semantics so HTTP-request-succeeded is not conflated with vulnerability-confirmed — done in P1 (`reconforge/attack_paths/engine.py`: `unreachable`/`reachable`/`corroborated` tiers replacing the binary flag) — §7
- [x] Replace hardcoded confidence constants in `reconforge/intelligence/engine.py` with evidence-derived scoring — done in P1 (`_evidence_confidence()`, per-rule concrete evidence factors, `confidence_reason` on every classification) — §7
- [x] Re-label or remove `docs/PROJECT_SCORECARD.md` and `docs/INDEPENDENT_SECURITY_ASSESSMENT_2026-04-13.md` as self-assessments — done in P0 — §6
- [ ] Widen ruff ruleset beyond syntax-error-only checks; enable mypy strict mode across the real package tree, not 3 files — §10.8
- [x] Pin GitHub Actions to commit SHA; add Dependabot/Renovate and a gitleaks step — done in P1 (`.github/workflows/quality-gates.yml`, `.github/dependabot.yml`, `.gitleaks.toml`) — Dependabot is already opening PRs as of this writing
- [x] Add typed exception usage at real `Runner` call sites instead of magic return-code checks — done in P1 (`core/exceptions.py`: `KillSwitchBlockedError`, `PolicyBlockedError`, `InvalidCommandError` added; `run_or_raise()` now maps all 6 failure sentinels; fixed a real bug found while wiring it — a `shlex.split()` `ValueError` on malformed string commands was raised outside its own try/except)
- [x] Encrypt credential vault / loot manager by default; move the key off the same path as the data — partially done in P1: files now `chmod 0600`, saving without encryption warns explicitly, and `RECONFORGE_VAULT_KEY`/`RECONFORGE_LOOT_KEY` env vars let the key be supplied out-of-band. The CLI's `--encrypt-loot` default was deliberately **not** flipped to on — that's a real default-behavior change across every module/workflow subcommand, left as an explicit separate decision — §4 S7
- [x] Close the gap where `--enforce-scope` is opt-in with no fallback authorization requirement — resolved 2026-07-12 (Phase 5): `reconforge/cli.py::require_authorization()` now refuses to dispatch any active run unless `--authorized-target`, `--lab-mode`, or a validated `--enforce-scope` is present, exempting only `--dry-run`. This does not change `--enforce-scope`'s own exact-string-match limitation (still open, see S4), but it closes the "ran with zero acknowledgement of authorization at all" gap. Deliberately released as a MAJOR version bump (`2.0.0`) since existing unattended scripts now need one of the three flags added — §4 S4, §8
- [x] Document that `--enforce-scope`'s command-execution-level re-validation (S2/S3) does not extend into HTTP redirects or DNS resolution performed *inside* wrapped CLI tools — resolved 2026-07-12 (Phase 5): added a dedicated "Scope Enforcement Does Not Follow Redirects for CLI Tool Wrappers" section to `docs/LIMITATIONS.md` explaining that `network`/`web`/`api` shell out to nikto/ffuf/gobuster/sqlmap/nuclei, which follow redirects with their own HTTP clients outside ReconForge's visibility — unlike the Burp MCP path, where every request (including mutated/discovered URLs) passes through `BurpMcpProvider._enforce_request_scope()` before being sent — §4 S3
- [x] Fix a real command-injection-adjacent bug in `modules/network/tools/smbclient.py::list_share_contents()` — resolved 2026-07-12 (Phase 6): an unsanitized `path` argument was interpolated directly into an smbclient `-c` batch-command string (`ls {path}/*`); since smbclient's batch mini-language treats `;` as a command separator, a `path` containing `;` could inject additional smbclient commands (`del`, `put`, etc.). Fixed by validating `path` through `core.runner.validate_arg()` before it reaches the batch string. The AD module's equivalent (`ad/tools/smbclient.py`) was checked and confirmed unaffected — it always passes the hardcoded literal `"dir"`, never user input
- [x] Fix plaintext credential leakage into logs for three credential formats `core/logger.py`'s redaction never covered — resolved 2026-07-12 (Phase 6): `-w <password>` (ldapsearch bind password / impacket), `-U user%password` (smbclient), and `DOMAIN/user:password` (bare positional impacket identity string) all reached command logs unredacted. Added three new patterns to `_SANITIZE_PATTERNS`; the identity-string pattern is deliberately scoped to require a domain-qualified prefix to avoid false-positive redaction of unrelated `host:port` tokens elsewhere in logs (verified with a dedicated non-corruption test)
- [x] Fix `ad/tools/nmap.py::dns_all_srv()` hardcoding `success=True` regardless of the underlying `dig` calls' actual outcome — resolved 2026-07-12 (Phase 6): now tracks every `dig` call's result and reports `success=False` if any of the 4 SRV-record lookups actually failed to execute (an empty SRV answer is still a successful `dig` invocation — only real execution failures like an unreachable DNS server now surface as failure)
- [x] Fix `Runner.run()`'s `output_file=` parameter silently clobbering a tool's own output file across 11 wrappers — resolved 2026-07-12 (Phase 6): `Runner.run()` unconditionally does `output_file.write_text(proc.stdout)` after the subprocess exits (`core/runner.py:364`), which overwrites whatever the tool itself already wrote via `-o`/`--log-json`/`-oJ` when that same path is also passed as `output_file=`. Confirmed empirically for `curl_tool.py` via a live local-HTTP-server integration test (curl's `-o` fully redirects the response away from stdout, so the file was being overwritten with an empty string on every run — traced downstream to `modules/web/phases/surface_discovery.py::_analyse_headers()` silently producing false-positive "missing security header" findings and never detecting `Server:` header disclosure). Fixed the same pattern (tool binary not installed in this dev environment to empirically re-verify, so fixed on documented flag semantics instead) in `whatweb.py`, `ffuf.py`/`ffuf_api.py`, `arjun_tool.py`, `wpscan.py`, `wafw00f.py` — all confirmed-or-plausible format mismatches between the tool's stdout and its own `-o` file — and, more defensively than strictly necessary, in `gobuster.py`, `nuclei.py`/`nuclei_api.py`, `httpx_tool.py` where the tool's own JSON/quiet mode likely already makes stdout mirror the file (removed anyway for consistency; `nikto.py`/`enum4linux_ng.py` already used two separate paths correctly and were left untouched as the reference pattern)
- [x] Replace magic returncode literals (`-1`, `-2`) in 8 tool-wrapper files with `core.runner`'s named `RC_*` sentinels — resolved 2026-07-12 (Phase 6): these literals collided with `RC_TIMEOUT=-1`/`RC_TOOL_NOT_FOUND=-2`'s reserved meanings (no active caller currently branches on these specific wrappers' returncodes, so this was a latent footgun, not an active misclassification bug). The 4 impacket-family `_tool_missing()` helpers now use `RC_TOOL_NOT_FOUND` (that's literally what they mean); `hydra.py`'s "brute-force not authorized" block now uses `RC_POLICY_BLOCKED` (it's an authorization gate, not a generic -1); `ffuf.py`/`ffuf_api.py`/`gobuster.py`'s "no wordlist resolved" case got a new dedicated constant, `RC_PRECONDITION_FAILED = -7`, since neither existing sentinel fit
- [x] Fix dead `self.tool_cfg`/`self._tool_cfg()` wiring in `bloodhound.py`, `ad/tools/ldapsearch.py`, `netexec.py`, and one method of `advanced_impacket.py` — resolved 2026-07-12 (Phase 6): each instantiated a `ToolConfig` in `__init__` but never called it, so `tools.yaml` timeout overrides were silently ignored despite every one of these files' docstring claiming timeouts are config-driven. Now call `effective_timeout()`/`mode_timeout()` consistently, matching the pattern already correct elsewhere (e.g. `curl_tool.py`, `ffuf.py`)
- [x] Fix `ad/tools/smbclient.py::test_sysvol_access()`/`test_netlogon_access()` silently discarding their `timeout` parameter instead of forwarding it to `test_share_access()` — resolved 2026-07-12 (Phase 6)
- [x] Remove redundant double command-logging in `modules/surface/tools/nmap_stealth.py` and `service_detector.py` — resolved 2026-07-12 (Phase 6): both called `self.logger.command(...)` immediately before `runner.run()`, which already logs the same (redacted) command internally — every surface-module scan command was being written to the log twice with no functional difference
- [x] Fix `api/parsers/nuclei_parser.py` crashing the entire JSONL parse on a single `"info": null` record — resolved 2026-07-13 (Phase 7): `entry.get("info", {})` returns `None`, not `{}`, when the key is explicitly `null`; the following `info.get(...)` calls then raised an uncaught `AttributeError`. Ported the `isinstance` guard already present in `web/parsers/nuclei_parser.py` for the same tool
- [x] Fix `ad/parsers/bloodhound_parser.py` assuming every parsed JSON list entry is a dict across all 5 `parse_*_json()` methods — resolved 2026-07-13 (Phase 7): a malformed/non-dict entry (e.g. a bare string from a corrupted export) raised an uncaught `AttributeError` on `entry.get(...)`, aborting the whole file's parse. Added `isinstance` guards to skip malformed entries instead of crashing
- [x] Fix `ad/parsers/nmap_parser.py`'s text-mode NSE script regex silently dropping the `"|_"`-prefixed final line of a multi-line script block — resolved 2026-07-13 (Phase 7): nmap's text output marks the last continuation line with `"|_"` (no space) rather than `"| "`, which the old regex never matched. Confirmed live-reachable via `ad/collectors/dns_collector.py`'s fallback to text parsing when XML output isn't available — this is exactly where SMB-signing status commonly appears (`smb2-security-mode`'s final line)
- [x] Fix `network/parsers/nmap_parser.py` missing `OSError` from its caught-exceptions tuple (had `FileNotFoundError` but not the broader `OSError`, unlike its `ad/` sibling) — resolved 2026-07-13 (Phase 7): `PermissionError`/`IsADirectoryError` could previously propagate uncaught out of `parse_xml()`
- [x] Fix `ad/parsers/nmap_parser.py::parse_xml()` never populating its `raw` field on a parse-failure path, leaving zero postmortem-debugging signal on malformed XML — resolved 2026-07-13 (Phase 7): now reads the file text first (preserving it in `raw` regardless of what happens next) before attempting to parse it
- [x] Route `api/parsers/nuclei_parser.py`'s severity classification through the same shared `normalize_severity()` helper `web/parsers/nuclei_parser.py` already uses for the identical tool — resolved 2026-07-13 (Phase 7): the API variant's own bare `SEVERITY_MAP` had no alias table, so `"moderate"`/`"important"`/`"crit"` all silently fell through to `"info"` while the web variant correctly normalized them
- [x] Fix `network/parsers/smb_parser.py`'s narrower access-denied detection risking misclassifying denied SMB access as successful — resolved 2026-07-13 (Phase 7): ported `ad/parsers/smb_parser.py`'s broader `NT_STATUS_ACCOUNT_DISABLED`/bare `ACCESS_DENIED`/`LOGON_FAILURE` pattern coverage
- [x] Consolidate the triplicated LDIF entry-splitting algorithm — resolved 2026-07-13 (Phase 7), **scoped down from the original 3-way plan**: `ad/parsers/ldap_parser.py::_split_entries` and `ad/parsers/delegation_parser.py::_split_ldap_entries` were byte-for-byte identical (the latter's own docstring said "Replicates the pattern from ADLdapParser._split_entries") — extracted into `modules/ad/parsers/ldif_utils.py::split_ldif_entries()`, both now delegate to it. `network/parsers/ldap_parser.py::_parse_objects` was **not** consolidated: discovered during implementation that it preserves original attribute-name casing and ~15 call sites in that file do case-sensitive lookups (`obj.get("sAMAccountName")`, `obj.get("dNSHostName")`, `obj.get("operatingSystem")`), which the shared function's lowercased-key convention would silently break. Forcing a 3-way merge would have meant either rewriting every case-sensitive call site or adding a case-mode parameter — a materially larger, riskier change than the two truly-identical copies warranted, so the network variant was deliberately left as its own, differently-cased implementation
- [x] Fix `web/phases/exploit_candidates.py::_run_sqlmap()` fabricating a critical/confirmed SQLi finding from sqlmap's own negative-result lines — resolved 2026-07-13 (Phase 8): a bare `"injectable" in line_lower` check tripped on sqlmap's own negation phrasing ("does not appear to be injectable", "does not seem to be injectable"), reporting a target as vulnerable that sqlmap explicitly said was not. The single most severe bug found in this phase — fabricates the model's highest-trust tier from a parsing defect. Now guarded against sqlmap's known negative phrasings; `"is vulnerable"` kept as a reliable positive-only signal since it has no known negated form
- [x] Fix `web/phases/vulnerability_scanning.py` deriving nuclei-finding confidence FROM severity, inverting `_clamp_severity()`'s intended flow — resolved 2026-07-13 (Phase 8): `confidence="high" if sev in ("critical","high") else "medium"` meant any finding severe enough to need the clamp cap also got confidence high enough to escape it, defeating the mechanism entirely. Confidence is now a flat `"high"` tied to nuclei's own deterministic template-match behavior (pass/fail via regex/status/word/DSL matchers), independent of the template's rated severity
- [x] Fix `ad/phases/bloodhound_collection.py`'s Domain-Admin attack-path finding overclaiming `confidence="confirmed"` for a graph-traversal inference — resolved 2026-07-13 (Phase 8): per `core/findings_manager.py`'s own docstring, "confirmed" means exploited/verified; a multi-hop BloodHound ACL/group-membership inference is neither. Relabeled to `"high"` with an explicit `confidence_reason` — the `critical` severity is unaffected since `"high"` and `"confirmed"` share the same (no) cap, only the overclaimed verification language is fixed
- [x] Add `confidence_reason: str` field to `Finding`/`FindingsManager.add()`/all 5 modules' `add_finding()` wrappers — resolved 2026-07-13 (Phase 8): populated at the 3 fixed call sites above as adoption examples. Broader adoption across the remaining ~90 confidence-assignment call sites is a real, large effort deliberately not attempted in this phase — see P2 below
- [x] Remove dead `core/findings_manager.py::_CONFIDENCE_RANK` constant (defined, never referenced anywhere) and wire the also-dead `get_heuristic_findings()` (zero callers repo-wide, not even inside its own class's `to_markdown()`) into `to_markdown()`'s output as a dedicated "Heuristic Findings" section — resolved 2026-07-13 (Phase 8)
- [x] Fix `quick_report.md`'s "Critical & High Findings" section silently hiding severity-clamped findings — resolved 2026-07-13 (Phase 8): the section filters by post-clamp severity, so a finding that started critical but got downgraded to medium/low due to weak confidence previously vanished from the headline summary with zero operator-facing signal (only visible in the separate full `findings.md`). Added a visible warning to all 4 affected modules' quick-report generation (`network`/`web`/`api`/`surface` — `ad` uses a different `core/reporting/`-based pipeline and was unaffected) when `FindingsManager.clamped_count` is nonzero
- [x] Fix `modules/surface/intelligence/confidence_scorer.py::ConfidenceScorer` never emitting the `"heuristic"` tier — resolved 2026-07-13 (Phase 8): its `LABELS` threshold table only produced confirmed/high/medium/low, so a zero-signal service detection (no port/banner/version/multi-detection/HTTP-confirm match at all — genuinely no concrete evidence) was mislabeled `"low"` instead of `"heuristic"`, `FindingsManager.VALID_CONFIDENCES`' weakest tier. Fixed an existing test (`tests/test_surface_intelligence.py`) that was asserting the old, incorrect behavior
- [x] Align `api/phases/discovery.py`'s ffuf-discovered-endpoint confidence with `web/phases/content_enumeration.py`'s tiering for the same evidence type — resolved 2026-07-13 (Phase 8): the API variant marked every ffuf hit `confidence="confirmed"` unconditionally regardless of HTTP status code, while the web variant (same tool, same kind of evidence) ties confidence to status code (`high` for 200/301/302, `medium` otherwise) — now aligned
- [x] Add test coverage for `core/findings_manager.py` — resolved 2026-07-13 (Phase 8): had zero tests prior to this phase, not even `_clamp_severity()`'s boundary cases. Added the full 25-combination confidence×severity clamp matrix plus coverage for `add()`'s validation/normalization/warning/clamping paths and every getter/aggregate/serialization method
- [x] Fix `network/phases/authentication_checks.py` reporting SMB null-session access twice for any dual-port (139+445) SMB host — resolved 2026-07-13 (Phase 9): `ANON_TEST_SERVICES` mapped both ports to the same host-level `null_session` test (the underlying `smbclient.null_session_test()` call takes no port argument), so the test ran — and a finding was recorded — once per matching port. The overwhelming majority of Windows/Samba targets have both ports open, making this a near-guaranteed duplicate on any real scan. Fixed by tracking whether the host-level test already ran this call; the finding's `target` is now the bare host (it was never actually port-specific)
- [x] Fix `network/parsers/nmap_parser.py::check_anonymous_access()` double-firing when both a service-name heuristic and NSE script output indicate anonymous access for the same port — resolved 2026-07-13 (Phase 9): the two signals were appended independently, producing two findings describing the identical condition. Now emits at most one finding per port, preferring the stronger NSE-script evidence when both fire
- [x] Add exact-match fingerprint deduplication to `FindingsManager.add()` — resolved 2026-07-13 (Phase 9): modeled directly on `core/credential_vault.py::CredentialVault._fingerprint()`'s proven O(1) set-based pattern. Fingerprint key is `finding_type|severity|confidence|target|description`, checked before `enrich_references()` (which can hit the network) so a repeated call does no wasted work. A duplicate `add()` call now returns the first-seen `Finding` instead of creating a second entry; exposed via a new `duplicate_count` property, surfaced in `to_markdown()`. Deliberately narrow — catches the same call being made twice (exactly the class of bug fixed in the two items above), not semantically-similar findings worded differently by different tools, which remains open (see P2 below)
- [x] Fix `modules/surface/intelligence/correlation_engine.py::_ingest_http()` grouping HTTP services by scheme alone, ignoring port — resolved 2026-07-13 (Phase 9): two genuinely distinct HTTP services on different ports of the same host (e.g. `:80` and `:8080`) were conflated into a single `CorrelatedService`, misattributing their `technologies`/`products`/`urls` to each other. Fixed to key by port, while still correctly merging a URL probe into an existing port-scan-detected entry for the same port when one exists — verified both behaviors empirically with direct calls, since the pre-existing test's own assertion (`"http://..." in http.urls or "http" in surface.services`) had a same-line `or` clause that would have silently passed either way regardless of whether the merge actually worked
- [x] Fix `CorrelationEngine._ingest_entry()` hardcoding `detection_method="port_scan"` for every entry, discarding `ServiceDeduplicator.deduplicate_ports()`'s real `_detection_methods` tagging on pre-merged entries — resolved 2026-07-13 (Phase 9): this meant `ConfidenceScorer`'s `multi_detection` signal (20% weight, `len(detection_methods) >= 2`) could never fire for any TCP/UDP service — only HTTP services could reach 2+ tags, via the separate `http_probe` path — systematically undercounting confidence for exactly the services the correlation/dedup pipeline exists to strengthen. Fixed to prefer the entry's own `_detection_methods` tagging when present
- [x] Wire the dead `WorkflowOrchestrator.findings` attribute for real cross-module finding aggregation — resolved 2026-07-13 (Phase 9): it was instantiated under a `# Aggregated managers` comment but never written to or read anywhere in the repo, unlike `CredentialVault`/`LootManager`, which already flow through `credential_vault.ingest_from_loot(mod.loot)` after every module run in `_run_module()`. Added `FindingsManager.ingest(other)` (re-runs each finding through `add()`, so the new fingerprint dedup applies automatically to cross-module duplicates too) and wired it into `_run_module()`'s existing loot-ingestion call sites plus the workflow's final report save (`findings_<timestamp>.{json,md}` alongside the existing `vault_<timestamp>.json`)

### P2 — Important improvements
- [x] Consolidate `modules/network/tools/*` and `modules/ad/tools/*` duplicate wrappers — assessed 2026-07-12 (Phase 3), **not** consolidated: diffed `nmap.py`/`smbclient.py`/`ldapsearch.py` between the two modules. The overlap is a small (~10-15 line) "build the base command" pattern; the majority of each file is genuinely AD-specific (SYSVOL/NETLOGON/admin-share checks, LDAP/Kerberos NSE script sets) or general-network-specific higher-level methods with no equivalent on the other side. Forcing a shared base class would touch tested, working code across both modules for a thin, low-risk shared fragment — judged not worth it relative to effort/risk. Revisit only if a real bug requires fixing the same logic twice — §5
- [ ] Add dedicated parser modules + tests for sqlmap, hydra, httpx inline-parsed adapters — §9
- [ ] Apply `validate_arg()` consistently across all tool wrappers, not just `modules/api/*` — re-confirmed 2026-07-12 (Phase 6): exactly 4 of the 28 `modules/*/tools/*.py` wrappers call it (all in `modules/api/tools/`: `arjun_tool.py`, `ffuf_api.py`, `httpx_tool.py`, `nuclei_api.py`). The other 24 — including the highest-traffic ones (`network/tools/nmap.py`, `ad/tools/nmap.py`, `surface/tools/nmap_stealth.py`) and every AD credential-bearing wrapper (target/domain/username/password/dc_ip) — build commands from unvalidated input. Not fixed in Phase 6: this is a genuinely large, mechanical change (24 files × several call sites each) better scoped as its own pass so each file's validation additions get individually reviewed rather than bulk-applied — §4 S6
- [ ] `modules/api/tools/httpx_tool.py` and `modules/surface/tools/service_detector.py` (`TOOL_CONFIG_KEY = "httpx_surface"`) independently wrap httpx with near-identical flag sets (`-json -tech-detect -status-code -follow-redirects`), a third instance of the network/ad/smbclient-style per-module tool duplication already assessed in Phase 3-E. Not consolidated in Phase 6 for the same reason Phase 3-E gave for the nmap/ldapsearch/smbclient trio (each caller's surrounding logic differs enough that a forced shared base would touch tested working code for a thin shared fragment) — flagged here rather than silently left unmentioned
- [~] Extend the `Finding` model with missing fields (CWE/CAPEC/MITRE ATT&CK, port/protocol/service, status, confidence_reason, raw_evidence_reference) — `confidence_reason` added 2026-07-13 (Phase 8), populated at 3 call sites as adoption examples; CWE/CAPEC/MITRE ATT&CK, port/protocol/service, status, and raw_evidence_reference remain open — §9
- [x] Add finding-level deduplication/correlation — resolved 2026-07-13 (Phase 9): `FindingsManager.add()` now does exact-match fingerprint dedup; `FindingsManager.ingest()` aggregates findings across modules with the same dedup applying. See §11 Phase 9 checklist below for the specifics and what's still open (cross-module *semantically-similar-but-differently-worded* duplicates are explicitly out of scope for the exact-match approach taken here)
- [ ] Extend attack-path graph to cover AD/network assets, not just HTTP endpoints — §9, still open after Phase 10 (§10 fixed correctness/dedup bugs in the existing HTTP-only engine and the AD builders, but did not merge the two into one graph)
- [x] `reconforge/attack_paths/engine.py` correctness bugs — resolved 2026-07-13 (Phase 10): `_path_impact()`'s dead `"auth_bypass"` string check (real type is `"auth_bypass_candidate"`), `_compatible_findings()`'s cluster allowlist missing `"auth"`/`"role"`, `_refine_paths()`'s self-revisit duplicate-step bug, and `_build_graph()`'s unguarded cluster→endpoint edges — see §10 in the phase log above for specifics
- [x] `core/attack_workflow.py::AttackWorkflow.add_attack_path()` had zero dedup (unlike `suggest_next()`) — resolved 2026-07-13 (Phase 10): exact-match dedup by chain name, same scoping choice as Phase 9's `FindingsManager` fingerprint dedup (semantically-similar-but-differently-*named* chains are not caught — see the new P2 item below)
- [x] Two dead attack-path-builder instantiations (`self.acl_path_builder` in `passive_recon.py`, `self.delegation_builder` in `bloodhound_collection.py` — both constructed, `.build()` never called) — resolved 2026-07-13 (Phase 10): the ACL one was wired in (cheap, real data available); the delegation one was removed (genuinely dead — its job is already covered elsewhere in that phase, and the dedicated `delegation_discovery.py` phase already wires the same builder correctly with the right data)
- [x] `privilege_escalation_paths.py`'s hardcoded literal password (`'Spring2026!'`) in a suggested crackmapexec command — resolved 2026-07-13 (Phase 10): replaced with a `passwords.txt` wordlist reference
- [x] Stale `"ReconForge v1.0 release"` report-footer string hardcoded independently in 8 files (vs. actual `2.2.0`) — resolved 2026-07-13 (Phase 10): added `core/version.py::__version__` as the one runtime-importable copy, wired into all 8, with a regression test keeping it in sync with `pyproject.toml`
- [ ] Cross-module/cross-phase attack-path chains that are semantically similar but differently *named* — noted 2026-07-13 (Phase 10) as the scoping limit of the new `add_attack_path()` dedup (exact-match on name only, mirroring Phase 9's `FindingsManager` scoping decision for the same reason). Not attempted — needs similarity/semantic matching, a materially larger effort than exact-match
- [ ] `modules/surface/intelligence/attack_prioritizer.py` remains a third, structurally isolated "attack"-adjacent system (target ranking, no chains/graph, no shared types with `reconforge/attack_paths/engine.py` or `modules/ad/attack_paths/*`) — confirmed 2026-07-13 (Phase 10) while auditing the other two; not consolidated for the same reason Phase 3-E/6 gave for other cross-module tool duplication (each is structurally different enough that forcing a shared abstraction isn't worth the risk to tested code)
- [x] `bloodhound_collection.py::_identify_da_paths()`'s Kerberoast/AS-REP→DA branches were dead (SID-vs-readable-name mismatch in `_is_privileged_group()`) — resolved 2026-07-13 (Phase 11): now checks membership against `results["da_users"]` (SID-keyed, built by `PrivilegeAnalyzer`); the unreachable helper was removed
- [x] `api/phases/fuzzing.py` misattributed batch-wide ffuf stdout evidence to individual entries' URLs — resolved 2026-07-13 (Phase 11): `_classify_error_response()`/`_check_for_info_disclosure()` now classify the batch once per fuzz run and report a single finding scoped to the endpoint under test, honest that the specific triggering input can't be isolated (ffuf's JSON output format captures no per-result response body)
- [x] `DelegationCollector.collect()` hardcoded `success = True` regardless of whether any query completed — resolved 2026-07-13 (Phase 11): each LDAP query and findDelegation.py now reports whether it actually ran; `delegation_discovery.py` surfaces an explicit finding and returns early on total collection failure instead of silently continuing on empty data
- [x] `web/phases/surface_discovery.py::_run_wafw00f()` was missing the `run_result.success` check every sibling tool-invoking method in the file has — resolved 2026-07-13 (Phase 11): added, matching `_run_whatweb()`'s existing pattern
- [ ] `results["success"]` is decorative (set `True` unconditionally) in 12 of 13 `modules/*/phases/*.py` files — surveyed 2026-07-13 (Phase 11): `bloodhound_collection.py` and now `delegation_discovery.py` are the only two where it reflects a real outcome. Confirmed via repo-wide grep that no current caller (`ad_module.py`/`web_module.py`/`api_module.py`/`workflow_orchestrator.py`) reads any phase's `results["success"]` today, so this is inert rather than actively misleading — but it's a latent trap for any future reporting/orchestration code that starts trusting the field name's implication. Making all 12 honest is a large, phase-by-phase mechanical pass (each needs its own judgment call on what "success" should mean for that phase's mix of tools) — not attempted wholesale here, only the 2 files whose false success signal was already being actively investigated
- [ ] `web/phases/vulnerability_scanning.py`'s SSRF-confirmation regex (`connection refused|failed to connect|localhost|127\.0\.0\.1`) matches anywhere in the response body with no adjacency check to the reflected probe value — noted 2026-07-13 (Phase 11). Already hedged `confidence="low"`, so not a fabrication in the severe sense of other findings in this codebase, just weak signal-to-noise (a generic URL-validation error or unrelated page text containing "localhost" would also match). Not tightened in this phase — low severity, and requires the same page-position/proximity logic to fix properly that wasn't otherwise in scope
- [x] `modules/surface/parsers/surface_parser.py`'s silent swallow fixed (narrowed + surfaced via `result.raw_output`); `core/secrets_manager.py:48`'s `except Exception: self._file_cache = {}` is still open — §4 S8
- [ ] Enable risk policy enforcement by default (or document clearly why it defaults off) — §4 S10
- [x] Add `[tool.coverage]` config — done, but the codified floor is an honest 50% (real coverage), not 85% (never true) — see P0 §11
- [x] Add SECURITY.md — done. CODE_OF_CONDUCT.md still open.
- [ ] `docs/DOCUMENTATION_INDEX.md` is stale (says "20 Markdown documents," last updated 2026-03-21 — many docs added/changed since, including this one, SECURITY.md, LICENSE) — noticed during Phase 3, not fixed; scoped better as Phase 22 (Documentation) work than a quick edit
- [ ] Severity/confidence assignment is systemically inconsistent across the 26 `modules/*/parsers/*.py` files — surveyed 2026-07-13 (Phase 7): `web/parsers/severity.py`'s `normalize_severity()` alias table is used by exactly 2 files (`web/parsers/nuclei_parser.py` and, as of this phase, `api/parsers/nuclei_parser.py`); every other severity-assigning parser reimplements its own scheme — hardcoded keyword-tuple matching against free text (`nikto_parser.py`, `wpscan_parser.py`, `arjun_parser.py`), a hardcoded per-CVE literal dict (`network/parsers/nmap_parser.py`), or structural boolean-flag-driven strings with no evidence trail (`delegation_parser.py`). None of these follow the evidence-derived-confidence-with-reason pattern `reconforge/intelligence/engine.py` got in P1-5 (`_evidence_confidence()` → `(confidence, reason)`); the parsing layer feeding that intelligence layer never got the same treatment. Promoting `normalize_severity()` to a module-agnostic location so `network`/`ad` parsers can reach it, and designing an equivalent evidence-derived confidence helper for parser-level classifications, is a real but large effort — not attempted here
- [ ] Return-type convention is ad hoc across the 26 parsers — surveyed 2026-07-13 (Phase 7): some return a dataclass with both `raw_output` and an `errors: List[str]` field (`network/parsers/ldap_parser.py`, `network/parsers/enum4linux_parser.py`, `api/parsers/openapi_parser.py` — most mature of the batch), some return a dataclass with `raw`/`raw_output` but no errors field, and some return a bare `List[dataclass]` or `Dict[str, list]` per query type with no wrapping result object at all and nowhere to record a raw/error signal (`ad/parsers/ldap_parser.py`, `ad/parsers/impacket_parser.py`, `ad/parsers/netexec_parser.py`, `ad/parsers/delegation_parser.py`). Standardizing on the `errors`-field convention across at least those four files is a real follow-up, deliberately not attempted here since it changes public return shapes touched by multiple call sites each
- [ ] `ad/parsers/netexec_parser.py`, `ad/parsers/delegation_parser.py`, and `ad/parsers/impacket_parser.py` still have no raw-output/error-signal preservation on parse failure (same class of gap fixed for `bloodhound_parser.py` and `ad/parsers/nmap_parser.py` in this phase) — not fixed here because, per the item above, these three return bare `List`/`Dict` types with nowhere to attach a raw/error field without a wider return-type change
- [ ] `ad/parsers/impacket_parser.py::parse_getadusers()`'s whitespace-column-splitting (`re.split(r"\s{2,}", line)`) assumes ≥2 spaces reliably separate GetADUsers.py's fixed-width table columns — noted 2026-07-13 (Phase 7) as an unconfirmed heuristic risk: a blank optional column (e.g. empty `description`) could collapse the whitespace gap and silently shift subsequent fields by one position. Not fixed speculatively — no real `GetADUsers.py` output sample with blank optional columns was available to verify the failure mode or test a fix against
- [ ] AD module's wholesale `confidence="confirmed"` usage — surveyed 2026-07-13 (Phase 8): all ~30 `add_finding`/`self.findings.add` call sites across `ad/phases/*.py` and `ad/analyzers/*.py` use `confidence="confirmed"` with zero exceptions, including the one fixed in this phase (bloodhound DA-paths). The remainder are mostly directly-observed AD/LDAP/SMB facts (password policy, delegation flags, SPN presence, SMB signing, anonymous LDAP) — defensible as "confirmed" in a loose "directly observed" sense even though they're not "exploited," per a stricter reading of the docstring — plus some purely descriptive/informational findings ("N users collected") where the confidence field carries no real signal either way. Relabeling all ~29 remaining sites is a large, judgment-heavy pass (each needs a case-by-case call, not a mechanical find/replace) — deliberately not attempted wholesale in this phase, only the one clearly-wrong critical/graph-inference case was fixed
- [ ] `modules/surface/intelligence/confidence_scorer.py::ConfidenceScorer` — surveyed 2026-07-13 (Phase 8): the closest thing to a principled, multi-signal, explainable confidence model in the recon-module codebase (weighted boolean signals → score → human-readable explanation, structurally similar to `reconforge/intelligence/engine.py`'s P1-5 `_evidence_confidence()` pattern), but scoped only to the surface module — confirmed via repo-wide grep that `network`/`ad`/`web`/`api` never import it. Promoting it (or an equivalent) to a shared location so the other 4 modules could use it instead of ad hoc keyword-list/magic-literal confidence assignment is a real, large effort — not attempted here
- [ ] A handful of other within-file confidence inconsistencies spotted but not fixed in Phase 8 (lower-severity than the 3 fixed — no fabricated findings, just inconsistent tiering for equally-strong evidence): `web/phases/exploit_candidates.py` marks a wpscan-parsed WordPress version `confirmed` but a wpscan-parsed plugin/theme inventory (same tool, same direct-JSON-parse evidence) only `high`; `api/phases/authentication.py` marks a JWT `alg=none` header `confirmed` but an equally-directly-parsed empty/trivial JWT signature only `high`
- [ ] `core/cve_enricher.py::enrich_references()` (called unconditionally inside `FindingsManager.add()`) can trigger a blocking network call (4s timeout) to the live NVD API when `RECONFORGE_NVD_LOOKUP=1` is set and a CPE string appears in a finding's description/evidence — surveyed 2026-07-13 (Phase 8). `.add()` is called in tight per-share/per-user/per-port loops throughout phase code with no visible indication in `findings_manager.py` that it can have this side effect, no caching beyond the on-disk CVE cache, and no rate-limiting. Off by default (env-gated), so not an active bug today, but a latent performance/reliability landmine if the env var is ever set in a real engagement — not restructured in this phase
- [ ] Cross-module semantically-similar (not byte-identical) duplicate findings — surveyed 2026-07-13 (Phase 9): `network/parsers/nmap_parser.py::check_weak_configs()` and `ad/analyzers/permission_analyzer.py` each independently derive and report SMB-signing status for the same target from their own nmap NSE runs during a `WorkflowOrchestrator` auto-handoff (network → ad), but with different description wording ("SMB message signing is disabled" vs. "SMB signing is not required — relay attack possible") — `FindingsManager`'s new exact-match fingerprint dedup (this phase) does not catch this, since the fingerprint includes the literal description string. Catching worded-differently duplicates needs similarity/semantic matching (embedding-based or keyword-overlap), not exact-match fingerprinting — a materially different, larger effort than what this phase built, not attempted here
- [ ] `core/credential_vault.py::CredentialVault._fingerprint()` does case-sensitive matching on `username`/`domain` — noted 2026-07-13 (Phase 9) while using it as the model for `FindingsManager`'s new dedup: `Administrator` vs. `administrator` from two different tools would not be deduped, understating true dedup effectiveness. Minor, not fixed here since it's pre-existing behavior in a file this phase didn't otherwise touch
- [ ] `core/loot_manager.py::LootManager.add()`'s O(n) linear-scan dedup (compares only `(loot_type, value)`, ignoring `module`/`source`/`confidence`) vs. `CredentialVault`'s O(1) set-based fingerprint — surveyed 2026-07-13 (Phase 9). Will degrade on long engagements with thousands of loot items; also silently coalesces a later, possibly higher-confidence duplicate report into the first-seen entry. Not restructured in this phase since `LootManager` itself wasn't in scope
- [ ] `network/phases/port_scanning.py`'s host-processing loop (`for host in nmap_result.live_hosts: if host.ip == target or not host_result["open_ports"]: self._process_host(...)`) has an edge case where, if the first host entry in nmap's XML output happens to have zero open ports, the loop condition remains true and a second, unrelated host entry can also be processed into the same `host_result` — noted 2026-07-13 (Phase 9) as a latent correctness issue found while investigating duplicate-finding scenarios, distinct from the dedup gap itself and low-likelihood for single-IP scans. Not fixed in this phase — out of scope for a dedup/correlation pass

### P3 — Optional enhancements
- [ ] Add framework/tool version + config-hash provenance to reports — `Runner.get_tool_version()` now exists (Phase 4) but no report/finding actually calls it yet
- [ ] Remove the 29 tracked PDF duplicates of Markdown docs from git
- [ ] Add packaging extras (`reconforge[ad]`, `reconforge[web]`, `reconforge[api]`)
- [ ] Add a documentation link checker and Docker lint job (once/if a Dockerfile is added)
- [ ] True streaming output-size enforcement in `core/runner.py` — current truncation is post-capture (subprocess.run already buffered the full output in memory first); real enforcement needs Popen + incremental chunked reads
- [ ] Wire `Runner.get_tool_version()` into tool wrapper constructors (~30 files) so findings/evidence can record which tool version produced them — re-confirmed 2026-07-12 (Phase 6): `grep -rn "get_tool_version" modules/ core/` still returns only the Phase 4 definition itself, zero call sites

---

## 12. Method Note

This review was produced by four parallel, independent research passes over the repository (subprocess/credential security; architecture and duplication; scope/findings/attack-path/reporting; documentation/licensing/CI), followed by direct verification of the highest-severity claims (`.abacus.donotdelete` file inspection, `core/target_parser.py` source read, README license text, `reconforge.py` reference count, and live `pytest`/`mypy`/`bandit`/`ruff` runs). Findings are cited with file:line references throughout; anything not independently re-verified in this pass is marked as such in §6 and should be re-checked before being cited externally.
