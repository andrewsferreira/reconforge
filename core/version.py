"""Single source of truth for ReconForge's version string used at runtime
(report footers, etc). Kept in sync with `pyproject.toml`'s `[project] →
version` by hand — see docs/VERSIONING.md's Release Process."""

__version__ = "2.14.3"
